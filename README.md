# Al-Fahd TV

This repository contains the current Al-Fahd TV sources.

- `android/`: Android application and its Node backend.
- `website/`: mobile-first website deployed separately.
- `live-proxy-worker/`: protected Cloudflare gateway for authorized live streams.

Secrets, local Gradle caches, and generated builds are intentionally excluded from version control.
